# Ollama runs locally by default (http://localhost:11434) — no API key needed.
# If your Ollama instance runs elsewhere, the ollama Python client reads
# OLLAMA_HOST from the environment:
# OLLAMA_HOST=http://localhost:11434